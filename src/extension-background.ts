/**
 * Give Me Doc — Extension Background Service Worker
 *
 * Currently a minimal stub. The popup handles all UI, and the content
 * script loads WASM directly from web_accessible_resources.
 */
import browser from 'webextension-polyfill';

console.log('[GiveMeDoc] Background service worker loaded ✓');
