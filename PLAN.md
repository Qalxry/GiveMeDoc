# PLAN

## 初稿

根据提供的资料，写一个插件。名称为 "Give Me Doc"，功能是将AI对话内容转换为Word文档。
插件将支持油猴脚本和浏览器扩展（Microsoft Edge、Firefox、Chrome）两种分发方式。
核心功能代码将被多个版本共享，以确保维护和扩展的便利性。

功能：
- 在DeepSeek网页注入几个按钮。具体功能见 `.vibe/llms_txt/deepseek.llms-full.txt.md` 文件。
- 使用pandoc.wasm将md转为word文档并下载。
- 需要支持配置word模板，用户可以上传自己的模板，插件会使用这个模板生成word文档。
- 在生成word前，需要对md进行格式化，确保生成的word文档格式正确。

分发：
- 油猴脚本版本：pandoc.wasm需要通过CDN加载，用户需要在油猴脚本设置里配置CDN地址。（默认提供一个CDN地址，需跨域访问： `https://pandoc.org/app/pandoc.wasm?sha1=2ab8055eb0803168da93d4b784fe40aa06551dfa` ）
- Microsoft Edge extension 版本：直接将 `./bin/pandoc.wasm` 文件打包在扩展里，无需用户配置CDN地址。
- Firefox extension 版本：同上。
- Chrome extension 版本：同上。

UI布局：
- 油猴脚本：在油猴脚本菜单里添加一个选项，点击后弹出panel，panel里有上传模板的功能和生成word的按钮。
- 浏览器扩展：在popup界面展示panel（复用同一个panel组件），panel里有上传模板的功能和生成word的按钮。
- 网页：注入按钮。使用lucide icons作为按钮图标。按钮功能如下：
  - 单条对话生成word：通过从剪贴板获取md内容，生成word文档。
  - 利用DeepSeek的对话选择功能，选择多条对话生成word文档。

技术栈：
- Typescript
- MDUI2
- Lucide icons
- Pandoc.wasm

要求：
- 代码结构清晰，易于维护和扩展。
- 未来可轻松适配其他平台（如Gemini、ChatGPT等）。
- 轻松构建多种分发版本（油猴脚本、浏览器扩展等）。
- 多种分发版本共享核心功能代码，避免重复开发。
- 尽可能使用 Shadcn Vue 提供的组件，保持UI一致性和高质量的用户体验。
- Comlink 用于在主线程和Web Worker之间进行通信，确保UI的流畅性和响应速度。
- 使用 PNPM 作为包管理工具，确保依赖管理的高效和一致性。
- Monorepo 目录结构设计
- 不过度设计，但也不耦合，保持代码逻辑清晰、模块化，易于维护和扩展。


增强导出仅作为一个消息选择器，底层还是要靠API/IndexDB。复制按钮就直接通过触发复制后从剪贴板获取即可。而API/IndexDB则需要具有检查IndexDB消息是否满足需求的功能，作为一个内部的小优化，减少API发送。

默认显示当前活跃分支，然后用户可以自己手动切换各个节点上后续接什么分支，类似chatgpt的聊天那样。注意某个节点切换分支后，后续消息都需刷新。需要支持重置为当前活跃分支的功能。这样无论获取的消息是树状的还是链状的，都可以适配。只有具有分支时，才显示 < {num} > 组件。



**√已提出4个问题(获取MD方式,思考内容,Word模板,对话树结构)**

> DeepSeek调研文档中列出了5种获取聊天MD的方式。你计划优先采用哪种(或哪几种)?我建议:主力用API方式(方式2),因为数据最完整;触发复制按钮(方式4)作为单条消息导出的快捷方式;增强导出功能(方式5)作为多选导出入口。:

增强导出仅作为一个消息选择器,底层还是要靠API/IndexDB。复制按钮就直接通过触发复制后从剪贴板获取即可。而API/IndexDB则需要具有检查IndexDB消息是否满足需求的功能,作为一个内部的小优化,减少API发送。

> DeepSeek的THINK fragment(模型思考过程)是否需要导出到Word中?如果导出,如何呈现?:
可选导出,默认不导出-用户可在设置中开关,默认不包含思考内容

> 关于Word模板(reference-doc),你希望提供什么级别的支持?:
提供多个预设模板+允许自定义-内置多套风格模板(学术、简约、商务等)

> DeepSeek用树状消息结构(有分支对话/regenerate的情况)。导出时如何处理?:
默认显示当前活跃分支,然后用户可以自己手动切换各个节点上后续接什么分支,类似chatgpt的聊天那样。注意某个节点切换分支后,后续消息都需刷新。需要支持重置为当前活跃分支的功能。这样无论获取的消息是树状的还是链状的,都可以适配。只有具有分支时,才显示<{num}>组件。

**√已提出4个问题(MD格式化,油猴CDN,扩展平台,Comlink用法)**

> 你提到生成Word前需要对MD进行格式化。具体需要哪些格式化操作?例如:修复标题层级、添加YAML front matter(标题/日期等元数据)、统一数学公式格式、代码块处理等。:
基本格式标准化-修复标题层级、统一列表格式、处理数学公式分隔符等

> 油猴脚本版本中pandoc.wasm的加载策略?默认CDN地址是否可配置?是否需要fallback?:
可同时配置多个CDN,默认先用着这个CDN,后续我会补充其他的一些源

> 初稿提到未来要适配Gemini/ChatGPT等平台。你希望如何设计平台抽象层,使新平台接入最简单?:
Adapter模式:平台适配层+共享核心:每个平台一个独立的适配层模块(adapter pattern)。确保相互之间不耦合、可灵活扩展。

> Comlink用于主线程和Web Worker通信。具体哪些操作需要放到Worker中?:
仅pandoc.wasm转换在Worker中。pandoc.wasm在Worker中运行,通过Comlink暴露convert/query API


- 导出页：显示消息列表，支持多选、搜索、分支切换等功能。提供模板选择按钮、导出按钮（旁边带有导出格式下拉栏，但目前只有word选项），点击后根据用户选择的消息和设置生成Word文档。
- 设置页：模板管理、导出选项配置、CDN设置
- 关于页：展示项目介绍、开发者信息、联系方式、状态信息

整体采用 **顶层标签页（Tabs）** 的布局。

以下是针对这三个页面的详细 UI 设计：

### 1. 整体外壳 (Panel Shell)

- 顶部固定导航栏：[ 导出 ] [ 设置 ] [ 关于 ]

### 2. 导出页 (Export Tab)

这是最核心的高频操作页面，要求操作顺滑、信息层级清晰。

**UI 布局自上而下：**

1. **顶部操作区 (Top Bar)**:
- **全选/反选**：一个简单的 `Checkbox`，旁边带有文字提示。

1. **消息列表区 (Message List)**:
- **容器**：使用滚动容器。
- **单条消息 (Card/List Item)**：
- 左侧：单选 `checkbox`。
- 中间：消息内容的摘要（单行截断 `truncate`）。
- 右侧：如果 DeepSeek 抓取到了分支（比如 2/3），使用一个小徽章 `badge` 和左右切换箭头（Lucide 的 `ChevronLeft`, `ChevronRight`）来实现**分支切换**。

1. **底部操作区 (Bottom Bar) - 固定在底部**:
- **模板选择器**：使用 `select` 组件，列出用户在设置页上传的模板（默认选中“默认模板”）。
- **导出按钮组**：这是一个组合按钮。
- 主按钮：使用 `button`（例如显示为“导出 Word”，带 `FileText` 图标）。
- 下拉触发器：紧挨着主按钮的向下箭头，使用 `dropdown menu` 组件，目前里面只有一个不可用的占位选项或单显“Word (.docx)”，为以后支持 PDF、Markdown 预留空间。

---

### 3. 设置页 (Settings Tab)

主要由几个表单卡片组成，用户可以在这里管理模板、配置导出选项、设置CDN地址等。

**UI 布局自上而下：**

1. **模板管理 (Template Management)**:
- **列表**：显示当前已保存的模板名称列表。
- **操作**：每个模板旁边有“设为默认”和“删除 (`Trash2` 图标)”操作。
- **上传入口**：一个醒目的虚线框加上 `Upload` 图标，或者简单的 `Button` 调用文件选择器，用于上传 `.docx` 模板文件。（通过 IndexedDB 或扩展的 `chrome.storage.local` 存储模板的 Base64/ArrayBuffer）。


1. **导出选项 (Export Options)**:
- 使用 `Switch` (开关) 和 `Label` 组件。
- 例如：`[Switch] 包含对话元数据 (时间、角色)`、`[Switch] 保持代码块语法高亮风格` 等格式化相关的配置。


1. **高级设置 / CDN 配置 (Advanced / CDN)**:
- 如果是扩展环境：可以隐藏或置灰此项，并提示“扩展内置 Pandoc，无需配置”。
- 如果是油猴环境：显示一个 `Input` 框，标题为“Pandoc WASM CDN 地址”，并提供一个“恢复默认”的按钮。

---

### 4. 关于页 (About Tab)

用于展示状态和信息，建立信任感。

**UI 布局自上而下：**

1. **系统状态 (System Status)**:
- 这非常关键。用一个小卡片 `Card` 显示环境健康度。
- “当前运行环境”：（扩展版 / 油猴版）。
- “Pandoc 引擎状态”：用一个小绿点或者红点表示 WASM 是否加载成功（这对调试和用户排错非常有用）。

1. **项目信息 (Project Info)**:
- Logo / 图标。
- 项目名称、版本号 (v1.0.0)。
- 简短的介绍文本。

1. **链接与支持 (Links & Support)**:
- 使用文字链接或小按钮（带 `Github`, `Globe` 等图标）。
- 包含：GitHub 源码仓库、报告 Bug 链接、开发者主页等。


细粒度拆分 (core + ui + adapter + apps) - packages/core, packages/ui, packages/adapters, apps/userscript, apps/extensions

- 通用流程：Panel内选择消息->选择模板和选项（选择后会持久化该选择）->点击导出->调用核心逻辑生成文档->下载，文件名从API/IndexDB获取对话标题
- DeepSeek网页专用流程：
  - 单条消息导出：点击某条消息泡泡下方注入的导出为Word的图标按钮->JS模拟点击第一个复制按钮->从剪贴板获取MD内容->从内部获取选择的模板和配置->调用核心逻辑生成Word->下载，文件名从网页标题获取以避开API/IndexDB的逻辑。
  - 分享界面增强：点击网页自带的分享->进入网页自带的消息选择界面->用户使用网页自带的选择->点击注入的导出为Word的图标按钮（和分享按钮在同一栏）->检测哪些消息的checkbox被选择->从API/IndexDB获取当前活跃分支消息链->筛选需要导出的消息->从内部获取选择的模板和配置->调用核心逻辑生成Word->下载，文件名从API/IndexDB获取对话标题
- 其他平台流程：单条消息导出可能也可以配备相似的逻辑，但分享界面可能没有。


单条消息导出由于是通过复制按钮触发的，因此天生只能一个回复导出。


总前缀：（一个文本多行输入框）
```markdown
---
title: {title}
date: {output_date}
---

```

用户消息组织：（一个文本多行输入框）
```markdown
---
**用户：**

{content}

```

助手消息组织：（一个文本多行输入框）
```markdown
---
**助手：**

> {thinking_content} （如果思考按钮被关闭的话，这部分内容就置空并删除这里的一个空行）

{content}

```

引用块识别：`>` 开头，则插入的内容的每一行前面都会加上 `>`。如 `> {thinking_content}`。
支持的占位符：
- `{title}`：对话标题
- `{output_date}`：导出日期，格式为 YYYY-MM-DD HH:mm:ss
- `{content}`：消息内容
- `{thinking_content}`：THINK fragment内容（如果思考按钮被关闭的话，这部分内容就置空并删除这里的一个空行）